<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>eu.cessda.cmv</groupId>
	<artifactId>cmv-server</artifactId>
	<version>0.2.0-SNAPSHOT</version>
	<name>CESSDA Metadata Validator</name>
	<url>https://cmv-dev.cessda.eu</url>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.3.1.RELEASE</version>
	</parent>

	<properties>
		<docker.image.name>cessda/${project.artifactId}</docker.image.name>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>11</java.version>
		<code-generation-comment>This file is generated, all changes will be overriden</code-generation-comment>
		<logging.file.name>target/application.log</logging.file.name>
		<logging.level.org.springframework>INFO</logging.level.org.springframework>
		<server.port>8080</server.port>
		<server.servlet.context-path>/</server.servlet.context-path>
		<maven-jar-plugin.version>3.2.0</maven-jar-plugin.version>
		<vaadin.version>8.11.0</vaadin.version>
		<vaadin.servlet.productionMode>false</vaadin.servlet.productionMode>
		<sonar.exclusions>**/*View.java,**/*Component.java</sonar.exclusions>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.vaadin</groupId>
				<artifactId>vaadin-bom</artifactId>
				<version>${vaadin.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>vaadin-spring-boot-starter</artifactId>
		</dependency>
		<dependency>
		    <groupId>org.springframework.boot</groupId>
		    <artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>com.vaadin</groupId>
			<artifactId>vaadin-themes</artifactId>
		</dependency>
		<dependency>
		    <groupId>com.vaadin</groupId>
		    <artifactId>vaadin-sass-compiler</artifactId>
		    <version>0.9.13</version>
		</dependency>
		<dependency>
			<groupId>com.wcs.wcslib</groupId>
			<artifactId>wcslib-vaadin-widget-multifileupload</artifactId>
			<version>4.0</version>
		</dependency>
		<dependency>
		    <groupId>org.vaadin</groupId>
		    <artifactId>viritin</artifactId>
		    <version>2.4</version>
		</dependency>
		<dependency>
			<groupId>org.zalando</groupId>
			<artifactId>problem-spring-web</artifactId>
			<version>0.25.2</version>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-ui</artifactId>
			<version>1.3.9</version>
		</dependency>
		<dependency>
			<groupId>eu.cessda.cmv</groupId>
			<artifactId>cmv</artifactId>
			<version>0.2.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>eu.cessda.cmv</groupId>
			<artifactId>cmv-core</artifactId>
			<version>0.2.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>org.gesis.commons</groupId>
			<artifactId>commons-resource</artifactId>
			<version>0.3.0-SNAPSHOT</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.gesis.commons</groupId>
			<artifactId>commons-test</artifactId>
			<version>0.5.0</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-sources</id>
						<goals><goal>jar</goal></goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.2.0</version><!--$NO-MVN-MAN-VER$-->
				<executions>
					<execution>
						<id>javadoc-jar</id>
						<phase>package</phase>
						<goals><goal>jar</goal></goals>
						<configuration>
							<quiet>true</quiet>
							<charset>UTF-8</charset>
							<docencoding>UTF-8</docencoding>
							<docfilessubdirs>true</docfilessubdirs>
							<includeDependencySources>true</includeDependencySources>
							<!-- For includes or excludes use pattern groupId:artifactId, not java package names -->
							<dependencySourceIncludes>
								<dependencySourceInclude>org.gesis.commons:*</dependencySourceInclude>
							</dependencySourceIncludes>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>pl.project13.maven</groupId>
				<artifactId>git-commit-id-plugin</artifactId>
				<executions>
					<execution>
						<id>get-the-git-infos</id><goals><goal>revision</goal></goals>
					</execution>
				</executions>
				<configuration>
					<verbose>false</verbose>
					<failOnNoGitDirectory>false</failOnNoGitDirectory>
					<dotGitDirectory>${project.basedir}/.git</dotGitDirectory>
					<generateGitPropertiesFile>true</generateGitPropertiesFile>
					<generateGitPropertiesFilename>${project.build.outputDirectory}/git.properties</generateGitPropertiesFilename>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
							<goal>build-info</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.vaadin</groupId>
				<artifactId>vaadin-maven-plugin</artifactId>
				<version>${vaadin.version}</version>
				<configuration>
					<extraJvmArgs>-Xmx512M -Xss1024k</extraJvmArgs>
					<webappDirectory>${basedir}/target/classes/VAADIN/widgetsets</webappDirectory>
					<noServer>true</noServer>
					<persistentunitcachedir>${basedir}/target/tmp/gwt-unitCache</persistentunitcachedir>
					<compileReport>true</compileReport>
					<strict>true</strict>
				</configuration>
				<executions>
					<execution>
						<goals>
	                         <goal>resources</goal>
	                         <goal>update-widgetset</goal>
	                         <goal>compile</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>io.fabric8</groupId>
				<artifactId>docker-maven-plugin</artifactId>
				<version>0.33.0</version>
				<configuration>
					<images>
						<image>
							<name>${docker.registry.host}/${docker.image.name}:${docker.image.tag}</name>
							<build>
								<dockerFile>${project.basedir}/target/docker/context/application.Dockerfile</dockerFile>
								<assembly><descriptorRef>artifact</descriptorRef></assembly>
							</build>
							<run><ports><port>8080:8080</port></ports></run>
						</image>
					</images>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<phase>initialize</phase>
						<configuration>
							<!-- https://stackoverflow.com/a/44033580 -->
							<exportAntProperties>true</exportAntProperties>
							<target>
								<condition property="isSnapshot"><contains string="${project.version}" substring="SNAPSHOT"/></condition>
								<condition property="docker.registry.host" value="docker-private-snapshots.gesis.intra"><isset property="isSnapshot"/></condition>
								<property name="docker.registry.host" value="docker-private-releases.gesis.intra" />
								<condition property="docker.image.tag" value="${git.branch}"><isset property="isSnapshot"/></condition>
								<property name="docker.image.tag" value="${project.version}" />
							</target>
						</configuration>
						<goals><goal>run</goal></goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<argLine>@{argLine} -Dfile.encoding=UTF-8</argLine>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.5</version>
				<executions>
					<execution>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>test</phase>
						<goals><goal>report</goal></goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>5.3.2</version>
				<configuration>
					<!-- Specifies if the build should be failed if a CVSS score above a 
					     specified level is identified. The default is 11 which means since 
					     the CVSS scores are 0-10, by default the build will never fail. 
					     Score 10 means the highest risk-->
					<failBuildOnCVSS>10</failBuildOnCVSS>
				</configuration>
			</plugin>
		</plugins>
		<finalName>${project.artifactId}</finalName>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>application.properties</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<includes>
					<include>git.properties</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources/docker</directory>
				<filtering>true</filtering>
				<targetPath>${project.basedir}/target/docker/context</targetPath>
				<includes><include>**/*</include></includes>
			</resource>
			<resource>
				<directory>src/main/webapp/VAADIN</directory>
				<targetPath>${project.basedir}/target/classes/VAADIN</targetPath>
				<includes><include>**/*</include></includes>
			</resource>
		</resources>
	</build>

	<profiles>
		<profile>
			<id>docker-compose</id>
			<properties>
				<logging.file.name>application.log</logging.file.name>
				<server.servlet.context-path></server.servlet.context-path><!-- necessary for HEALTHCHECK -->
				<vaadin.servlet.productionMode>true</vaadin.servlet.productionMode>
			</properties>
		</profile>
	</profiles>

	<organization>
		<name>CESSDA ERIC</name>
		<url>https://www.cessda.eu/</url>
	</organization>
	<scm>
		<url>https://bitbucket.org/cessda/cessda.cmv.server</url>
	</scm>
	<ciManagement>
		<system>Jenkins</system>
		<url>https://jenkins.cessda.eu/</url>
	</ciManagement>
	<issueManagement>
		<url>https://bitbucket.org/cessda/cessda.cvm.server/issues</url>
		<system>Bitbucket</system>
	</issueManagement>

	<repositories>
		<repository>
			<id>cessda-nexus-snapshots</id>
			<name>CESSDA Nexus Snapshot Repository</name>
			<url>https://nexus.cessda.eu/repository/maven-snapshots</url>
		</repository>
		<repository>
			<id>cessda-nexus</id>
			<name>CESSDA Nexus Repository</name>
			<url>https://nexus.cessda.eu/repository/maven-releases</url>
		</repository>
		<repository>
			<id>gesis-public</id>
			<url>https://maven.gesis.org/repositories/gesis-public</url>
		</repository>
	</repositories>
	<distributionManagement>
		<repository>
			<id>cessda-nexus</id>
			<name>CESSDA Nexus Repository</name>
			<url>https://nexus.cessda.eu/repository/maven-releases</url>
		</repository>
		<snapshotRepository>
			<id>cessda-nexus-snapshot</id>
			<name>CESSDA Nexus Snapshot Repository</name>
			<url>https://nexus.cessda.eu/repository/maven-snapshots</url>
			<uniqueVersion>true</uniqueVersion>
		</snapshotRepository>
	</distributionManagement>
</project>
